# ======================================================
# RENDEREXPO AI STUDIO - Model Paths
# NOTE:
# - We are currently testing on RunPod (pod environment).
# - Therefore sd35_large_dir points to the pod's mounted model path.
# - Local laptop dev can change this back to a relative path later.
# ======================================================

# SD3.5 Large model folder (pod testing)
sd35_large_dir: "/workspace-data/models/sd35-large"

# Future custom finetunes will live here
renderexpo_ultra_dir: "models/renderexpo-ultra"   # future

# Future LoRA directory (assets)
lora_dir: "models/lora"                            # future

# ControlNet directory (if you want copies locally)
controlnet_dir: "models/controlnet"                # future
