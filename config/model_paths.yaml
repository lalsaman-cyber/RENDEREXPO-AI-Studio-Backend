# ======================================================
# RENDEREXPO AI STUDIO - Model Paths
# ======================================================

# ------------------------------------------------------
# SD3.5 Large model folder (existing text-to-image path)
# ------------------------------------------------------
sd35_large_dir: "/workspace-data/models/sd35-large"

# ------------------------------------------------------
# SDXL sketch engine (new sketch-only path)
# ------------------------------------------------------
# These can be local paths later if you download them locally.
# For now they can stay as model IDs.
sdxl_base_dir: "stabilityai/stable-diffusion-xl-base-1.0"
mistoline_controlnet_dir: "TheMistoAI/MistoLine"

# ------------------------------------------------------
# Existing / future asset folders
# ------------------------------------------------------
renderexpo_ultra_dir: "models/renderexpo-ultra"
lora_dir: "models/lora"
controlnet_dir: "models/controlnet"